import { StatusColorPipePipe } from './status-color.pipe';

describe('StatusColorPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StatusColorPipePipe();
    expect(pipe).toBeTruthy();
  });
});
