spring:
  datasource:
    url: jdbc:mysql://localhost:3306/file_system?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: InfiniRag@Flow99
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
      generate-ddl: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect