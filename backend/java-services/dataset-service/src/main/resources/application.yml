server:
  port: 8083

spring:
  application:
    name: dataset-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.ragflow.dataset: INFO

