spring:
  application:
    name: dataset-service

  datasource:
    url: jdbc:mysql://${MYSQL_HOST:mysql}:${MYSQL_PORT:3306}/${MYSQL_DATASET_DBNAME:rag_dataset}?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: ${MYSQL_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update   # use update while developing
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        show_sql: true