spring:
  datasource:
    url: jdbc:mysql://localhost:5455/rag_dataset?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: infini_rag_flow
    driver-class-name: com.mysql.cj.jdbc.Driver

    hikari:
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        show_sql: false
        format_sql: true