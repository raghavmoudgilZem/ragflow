import { LoggerService } from '@nestjs/common';
import { ErrorLogger } from '@nestjs-modules/ioredis/node_modules/@nestjs/terminus/dist/health-check/error-logger/error-logger.interface';
import { HealthCheckExecutor } from '@nestjs-modules/ioredis/node_modules/@nestjs/terminus/dist/health-check/health-check-executor.service';
import { type HealthCheckResult } from '@nestjs-modules/ioredis/node_modules/@nestjs/terminus/dist/health-check/health-check-result.interface';
import { type HealthIndicatorFunction } from '@nestjs-modules/ioredis/node_modules/@nestjs/terminus/dist/health-indicator';
/**
 * Handles Health Checks which can be used in
 * Controllers.
 */
export declare class HealthCheckService {
    private readonly healthCheckExecutor;
    private readonly errorLogger;
    private readonly logger;
    constructor(healthCheckExecutor: HealthCheckExecutor, errorLogger: ErrorLogger, logger: LoggerService);
    /**
     * Checks the given health indicators
     *
     * ```typescript
     *
     * healthCheckService.check([
     *   () => this.http.pingCheck('google', 'https://google.com'),
     * ]);
     *
     *
     * ```
     * @param healthIndicators The health indicators which should be checked
     */
    check(healthIndicators: HealthIndicatorFunction[]): Promise<HealthCheckResult>;
}
